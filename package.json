{
  "name": "campus-os",
  "version": "2.0.0",
  "private": true,
  "description": "CampusOS - Unified Academic Operating System",
  "scripts": {
    "dev": "npm --prefix frontend run dev",
    "share": "python3 -m uvicorn app.main:app --app-dir backend --host 127.0.0.1 --port 8000 & npm --prefix frontend run dev & sleep 2 && ./cloudflared tunnel --url http://localhost:5173",
    "build": "npm --prefix frontend install && npm --prefix frontend run build && rm -rf dist && mkdir -p dist && cp -r frontend/dist/* dist/",
    "preview": "npm --prefix frontend run preview",
    "test": "pytest backend/tests"
  },
  "dependencies": {
    "@types/three": "^0.185.4",
    "framer-motion": "^13.1.1",
    "lucide-react": "^1.34.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "recharts": "^3.10.1",
    "three": "^0.185.1"
  },
  "devDependencies": {
    "@types/react": "^18.3.3",
    "@types/react-dom": "^18.3.0",
    "@vitejs/plugin-react": "^4.3.1",
    "typescript": "^5.4.5",
    "vite": "^5.3.4"
  }
}
